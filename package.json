{
  "name": "luis-valle-web",
  "version": "1.0.0",
  "type": "module",
  "description": "Página web de trail running para Luis Valle",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.58.0",
    "vite": "^8.3.0",
    "wrangler": "^4.137.0"
  }
}
